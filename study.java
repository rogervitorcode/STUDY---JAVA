package javac;

public class study {
    /*
    COMANDO PACKAGE >> SEMPRE DEVE SER O MESMO NOME DO PROJETO.
    CASO, ALGUM ARQUIVO CLASS ESTIVER FORA DO PACKAGE, ELE NAO VAI LER O ARQUIVO.

    DIFERENÇA DE
    ********************public class -- (  indica que o codigo sera lido de qualquer modo )

    se tirar o public, sera um codigo default(

    *************private class ( SO PODE SER ACESSADO NO MESMO CLASS
    ex; main.java so pode ser usado nele mesmo.)

    ********protected class >> so pode ser acessado num mesmo package, em outro nao funciona

    OBS:so podemos definir apenas 1 classe como public, que é a principal
      ex:   public class study
      (nao confundir com o static void, nele pode sim usaro public)
      */
}
